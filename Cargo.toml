[package]
name = "slint-terminal"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
slint = "1"
alacritty_terminal = "0.26.0"
arboard = { version = "3.6.1", default-features = false }
[build-dependencies]
slint-build = "1"
